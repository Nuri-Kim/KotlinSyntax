package ex20221121

fun main() {
    // 5) 논리 연산자 : &&, ||, !

    // && => 양족 항이 모두 true 인 경우 결과 값으로 true 를 돌려준다.

    // || => 하나라도 true 면 결과 값으로 true 를 돌려준다.

    // !(not , 부정연산자) => true 앞에 기재할 경우 false로 바뀐다
}